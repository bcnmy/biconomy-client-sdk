import { expect } from 'chai'
import { erc4337RuntimeVersion } from '../src/Version'

describe('erc4337RuntimeVersion', () => {
  it('should be a string', async () => {
    // expect(erc4337RuntimeVersion).to.be.a('string')
    // expect(erc4337RuntimeVersion).to.equal(await require('../package.json').version)
  })
})

